# Competitive Programming

STL
```
Map:
    ans = {}
    ans[key] = val
```

Math
```
a x b = LCM (a, b) * GCD (a, b)
```

String
```
 Reverse a string: 
 string = string[::-1]
 string = "".join(reversed(string))
```
